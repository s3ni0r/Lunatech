db {
  default.driver=org.h2.Driver
  default.url="jdbc:h2:mem:test;INIT=CREATE SCHEMA IF NOT EXISTS public;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1"
  default.username=lunatech
  default.password=""
}